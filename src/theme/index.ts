import { theme } from 'antd';

// const { darkAlgorithm, compactAlgorithm } = theme;

export const customTheme = {
  token: {
    colorPrimary: 'dark',
    borderRadius: 40,
    fontFamily: 'unset',
    controlHeight: 32, //The height of the basic controls such as buttons and input boxes in Ant Design
    fontSize: 18,
  },
  algorithm: [],
};
